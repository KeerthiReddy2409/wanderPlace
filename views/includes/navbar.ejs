<nav class="navbar navbar-expand-lg bg-light border-bottom sticky-top shadow-sm">
  <div class="container-fluid px-3">

    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center fw-bold fs-4 text-primary text-decoration-none" href="/listings">
      <i class="fa-solid fa-compass me-2"></i> WanderMap
    </a>

    <!-- Hamburger -->
    <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse"
      data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false"
      aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Content -->
    <div class="collapse navbar-collapse" id="navbarContent">

      <!-- Left Nav -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link fw-semibold px-3 py-2 rounded-pill text-dark" href="/listings">
            <i class="fas fa-globe me-1"></i>Explore
          </a>
        </li>
      </ul>

      <!-- Desktop Search Centered -->
      <div class="d-none d-lg-flex flex-grow-1 justify-content-center px-3">
        <form method="GET" action="/listings" class="w-100" style="max-width: 400px;" role="search">
          <div class="input-group shadow-sm">
            <input type="text" name="destination" class="form-control border-0 bg-white px-4 py-2 fs-6"
              placeholder="Search destinations..."
              value="<%= typeof destination !== 'undefined' ? destination : '' %>"
              style="border-top-left-radius: 25px; border-bottom-left-radius: 25px; min-height: 45px;">
            <button class="btn btn-primary px-3" type="submit"
              style="border-top-right-radius: 25px; border-bottom-right-radius: 25px;">
              <i class="fas fa-search"></i>
            </button>
          </div>
        </form>
      </div>

      <!-- Mobile Dropdown Menu -->
      <div class="d-lg-none d-flex flex-column gap-3 mt-3">

        <!-- Search for Mobile -->
        <form method="GET" action="/listings" role="search" class="px-2">
          <div class="input-group shadow-sm">
            <input type="text" name="destination" class="form-control border-0 bg-white px-3 py-2"
              placeholder="Search destinations..."
              value="<%= typeof destination !== 'undefined' ? destination : '' %>"
              style="border-top-left-radius: 25px; border-bottom-left-radius: 25px;">
            <button class="btn btn-primary" type="submit"
              style="border-top-right-radius: 25px; border-bottom-right-radius: 25px;">
              <i class="fas fa-search"></i>
            </button>
          </div>
        </form>

        <!-- Add Listing -->
        <div class="px-2">
          <a href="/listings/new" class="btn btn-primary w-100 d-flex justify-content-center align-items-center rounded-pill">
            <i class="fas fa-plus me-1"></i> Add Listing
          </a>
        </div>

        <!-- Auth/User -->
        <div class="px-2">
          <% if (!currUser) { %>
            <div class="d-grid gap-2">
              <a class="btn btn-outline-secondary rounded-pill" href="/signup">Sign Up</a>
              <a class="btn btn-primary rounded-pill" href="/login">Log In</a>
            </div>
          <% } else { %>
            <ul class="list-group shadow-sm">
              <li class="list-group-item"><a href="/profile"><i class="fas fa-user me-2"></i>Profile</a></li>
              <li class="list-group-item"><a href="/my-listings"><i class="fas fa-list me-2"></i>My Listings</a></li>
              <li class="list-group-item"><a href="/profile#wishlist"><i class="fas fa-heart me-2"></i>Wishlist</a></li>
              <li class="list-group-item text-danger"><a href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Log Out</a></li>
            </ul>
          <% } %>
        </div>

      </div>

      <!-- Desktop Right: Add Listing + User/Auth Buttons -->
      <ul class="navbar-nav ms-auto d-none d-lg-flex align-items-center gap-2">
        <li class="nav-item">
          <a href="/listings/new" class="btn btn-outline-primary rounded-pill d-flex align-items-center">
            <i class="fas fa-plus me-1"></i> Add Listing
          </a>
        </li>

        <% if (!currUser) { %>
          <li class="nav-item">
            <a class="btn btn-outline-secondary rounded-pill px-3 py-2" href="/signup">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-primary rounded-pill px-3 py-2" href="/login">Log In</a>
          </li>
        <% } else { %>
          <li class="nav-item dropdown">
            <button class="btn btn-light rounded-circle d-flex align-items-center justify-content-center p-2"
              type="button" data-bs-toggle="dropdown" aria-expanded="false" style="width: 40px; height: 40px;">
              <i class="fas fa-user text-primary"></i>
            </button>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2">
              <li><a class="dropdown-item py-2" href="/profile"><i class="fas fa-user me-2"></i>Profile</a></li>
              <li><a class="dropdown-item py-2" href="/my-listings"><i class="fas fa-list me-2"></i>My Listings</a></li>
              <li><a class="dropdown-item py-2" href="/profile#wishlist"><i class="fas fa-heart me-2"></i>Wishlist</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item py-2 text-danger" href="/logout"><i class="fas fa-sign-out-alt me-2"></i>Log Out</a></li>
            </ul>
          </li>
        <% } %>
      </ul>

    </div>
  </div>
</nav>
